server:
  port: 8081
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hughob36/findJobIT.ai.git
          search-paths: config-data
